# Bazowy URL backendu, wstrzykiwany do klienta Axios (src/services/api.js).
# UWAGA: zmienne VITE_* są wypiekane podczas `npm run build` (build-time),
# nie czytane w runtime. W obrazie Docker wartość ustawia build-arg VITE_API_URL.
VITE_API_URL=http://localhost:8000/
